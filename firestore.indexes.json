{
  "indexes": [
    {
      "collectionGroup": "products",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "searchIds", "arrayConfig": "CONTAINS" },
        { "fieldPath": "lastSeenAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "products",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "searchIds", "arrayConfig": "CONTAINS" },
        { "fieldPath": "price", "order": "ASCENDING" }
      ]
    }
  ],
  "_comment": "Solo indices compuestos. Los de un campo (por ejemplo history.capturedAt) los crea Firestore automaticamente y declararlos hace fallar el deploy con 'this index is not necessary'.",
  "fieldOverrides": []
}
